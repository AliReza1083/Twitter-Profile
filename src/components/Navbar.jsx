import React from "react";
import Gradients from "./Gradients";

const Navbar = () => {
  return (
    <div className="fixed top-24 left-8 shadow-xl p-4 rounded-lg">
      <Gradients />
    </div>
  );
};

export default Navbar;
